<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/17.0.12"
            fx:controller="co.edu.uniquindio.logistica.ui.UserAdminController"
            prefWidth="900" prefHeight="550"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20"
              style="-fx-background-color: linear-gradient(to right, #AB47BC, #8E24AA);
                     -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);">
            <Label text="🧑‍💼 Panel de Usuario como Admin"
                   style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 1px;"/>
        </HBox>
    </top>

    <!-- CUERPO CENTRAL -->
    <center>
        <VBox spacing="30" alignment="CENTER" style="-fx-padding: 40;">
            
            <Label text="Acceso rápido a funciones de usuario con privilegios de administrador"
                   style="-fx-font-size: 16px; -fx-text-fill: #E0F7FA; -fx-font-weight: bold; -fx-opacity: 0.9;"/>

            <!-- Tarjetas de opciones -->
            <GridPane hgap="30" vgap="30" alignment="CENTER">
                <columnConstraints>
                    <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                    <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                </columnConstraints>
                <rowConstraints>
                    <RowConstraints vgrow="ALWAYS"/>
                </rowConstraints>

                <!-- Crear Envío -->
                <Button text="📦 Crear Envío" onAction="#handleCrearEnvioAdmin"
                        GridPane.columnIndex="0" GridPane.rowIndex="0"
                        style="-fx-background-color: linear-gradient(to bottom right, #42A5F5, #1E88E5);
                               -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold;
                               -fx-background-radius: 14; -fx-padding: 35 50;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);
                               -fx-cursor: hand;"
                        prefWidth="320" prefHeight="120"/>

                <!-- Ver Historial -->
                <Button text="📋 Ver Historial de Envíos" onAction="#handleVerHistorialAdmin"
                        GridPane.columnIndex="1" GridPane.rowIndex="0"
                        style="-fx-background-color: linear-gradient(to bottom right, #66BB6A, #43A047);
                               -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold;
                               -fx-background-radius: 14; -fx-padding: 35 50;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);
                               -fx-cursor: hand;"
                        prefWidth="320" prefHeight="120"/>
            </GridPane>

            <!-- Botón volver -->
            <Button text="↩️ Volver al Login" onAction="#handleVolverAdmin"
                    style="-fx-background-color: linear-gradient(to bottom right, #757575, #424242);
                           -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                           -fx-background-radius: 10; -fx-padding: 12 30;
                           -fx-cursor: hand;"/>
        </VBox>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15;">
            <Label text="© 2025 Plataforma Logística | Panel de Usuario - Modo Admin"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.8;"/>
        </HBox>
    </bottom>
</BorderPane>
