<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox alignment="CENTER" prefHeight="620" prefWidth="420" spacing="10" xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/17.0.12" fx:controller="co.edu.uniquindio.logistica.ui.CrearUsuarioController">

    <Label style="-fx-font-size: 18px; -fx-font-weight: bold;" text="Registrar Usuario" />

    <TextField fx:id="idField" promptText="Número de identificación (cédula)" />
    <TextField fx:id="nombreField" promptText="Nombre completo" />
    <TextField fx:id="emailField" promptText="Correo electrónico" />
    <TextField fx:id="telefonoField" promptText="Teléfono" />
    <PasswordField fx:id="passwordField" promptText="Contraseña" />
    <CheckBox fx:id="adminCheck" text="¿Administrador?" />

    <Separator />

    <!-- 🔹 Zona de Origen -->
    <Label style="-fx-font-weight: bold;" text="Zona de Origen (obligatoria)" />
    <ComboBox fx:id="zonaOrigenCombo" promptText="Seleccione una zona" />

    <Label text="Dirección de Origen" />
    <TextField fx:id="alias1Field" promptText="Alias (ej: Casa, Bodega)" />
    <TextField fx:id="detalle1Field" promptText="Dirección completa de origen" />

    <Separator />

    <!-- 🔹 Zona de Destino -->
    <Label style="-fx-font-weight: bold;" text="Zona de Destino (obligatoria)" />
    <ComboBox fx:id="zonaDestinoCombo" promptText="Seleccione una zona" />

    <Label text="Dirección de Destino" />
    <TextField fx:id="alias2Field" promptText="Alias (ej: Oficina, Cliente)" />
    <TextField fx:id="detalle2Field" promptText="Dirección completa de destino" />

    <Separator />

    <Button onAction="#handleRegistrar" prefHeight="25.0" prefWidth="116.0" style="-fx-background-color: #1976D2; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;" text="Registrar Usuario" />
    <Button onAction="#handleVolverLogin" style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;" text="↩️ Volver al Login" />
    <Label fx:id="mensajeLabel" />

</VBox>
