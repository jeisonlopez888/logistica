<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>


<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.AdminsController"
            prefWidth="1100" prefHeight="650"
            style="-fx-background-color: linear-gradient(to bottom right, #f4f6f8, #e3f2fd);">

    <!-- 🔹 ENCABEZADO -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20"
              style="-fx-background-color: #1976D2; -fx-padding: 20;">
            <Label text="⚙️ Panel de Administración"
                   style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
        </HBox>
    </top>

    <!-- 🔹 TABLA CENTRAL -->
    <center>
        <VBox spacing="15" alignment="CENTER" style="-fx-padding: 25;">
            <Label text="Gestión de Administradores"
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #0D47A1;"/>

            <TableView fx:id="usuariosTable"
                       prefHeight="420"
                       style="-fx-background-color: white; -fx-border-radius: 10; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.15), 10, 0, 0, 2);">
                <columns>
                    <TableColumn fx:id="idCol" text="Cédula" prefWidth="100"/>
                    <TableColumn fx:id="nombreCol" text="Nombre" prefWidth="180"/>
                    <TableColumn fx:id="emailCol" text="Correo Electrónico" prefWidth="220"/>
                    <TableColumn fx:id="telefonoCol" text="Teléfono" prefWidth="150"/>
                    <TableColumn fx:id="passwordCol" text="Contraseña" prefWidth="150"/>
                    <TableColumn fx:id="adminCol" text="Admin" prefWidth="100"/>
                </columns>
            </TableView>

            <Label fx:id="mensajeLabel" text=""
                   style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #1976D2;"/>
        </VBox>
    </center>

    <!-- 🔹 BOTONES ABAJO -->
    <bottom>
        <VBox spacing="10" style="-fx-padding: 15;">
            <HBox alignment="CENTER" spacing="15">
                <Button text="➕ Crear Usuario" onAction="#handleCrearUsuario"
                        style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>
                <Button text="✏️ Editar Usuario" onAction="#handleEditarUsuario"
                        style="-fx-background-color: #FFC107; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>
                <Button text="🗑️ Eliminar Usuario" onAction="#handleEliminarUsuario"
                        style="-fx-background-color: #F44336; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>
                <Button text="👤 Panel Usuario" onAction="#handleUser"
                        style="-fx-background-color: #26A69A; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>
                <Button text="↩️ Volver al Login" onAction="#handleVolver"
                        style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>
            </HBox>
        </VBox>
    </bottom>


</BorderPane>
