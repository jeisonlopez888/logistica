<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.AdminsController"
            prefWidth="1100" prefHeight="650"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="15"
              style="-fx-background-color: linear-gradient(to right, #FFB300, #F57C00);
                     -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);">
            <Label text="✏️ Gestión de Administradores"
                   style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 1px;"/>
        </HBox>
    </top>

    <!-- CONTENIDO CENTRAL -->
    <center>
        <VBox spacing="20" style="-fx-padding: 25;">
            
            <!-- Tarjeta de tabla -->
            <VBox spacing="15" style="-fx-background-color: rgba(255,255,255,0.95);
                                      -fx-background-radius: 15;
                                      -fx-padding: 25;
                                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 3);">
                
                <Label text="Lista de Administradores del Sistema"
                       style="-fx-font-size: 16px; -fx-text-fill: #37474F; -fx-font-weight: bold;"/>

                <TableView fx:id="usuarioTableView" prefHeight="400" style="-fx-border-radius: 10; -fx-background-radius: 10;">
                    <columns>
                        <TableColumn fx:id="idCol" text="ID" prefWidth="70"/>
                        <TableColumn fx:id="nombreCol" text="Nombre" prefWidth="160"/>
                        <TableColumn fx:id="emailCol" text="Correo Electrónico" prefWidth="220"/>
                        <TableColumn fx:id="telefonoCol" text="Teléfono" prefWidth="130"/>
                        <TableColumn fx:id="passwordCol" text="Contraseña" prefWidth="120"/>
                        <TableColumn fx:id="rolCol" text="Rol" prefWidth="120"/>
                        <TableColumn fx:id="direccionesCol" text="Direcciones" prefWidth="230"/>
                    </columns>
                </TableView>
            </VBox>

            <!-- Mensaje de estado -->
            <Label fx:id="mensajeLabel" style="-fx-font-weight: bold; -fx-font-size: 14px; -fx-text-fill: white;"/>

            <!-- Botones de acción -->
            <HBox alignment="CENTER" spacing="20" style="-fx-padding: 10 0 0 0;">
                <Button text="✏️ Editar Admin" onAction="#handleEditarUsuario"
                        style="-fx-background-color: linear-gradient(to bottom right, #42A5F5, #1E88E5);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 30;
                               -fx-effect: dropshadow(gaussian, rgba(30,136,229,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"/>

                <Button text="🗑️ Eliminar Admin" onAction="#handleEliminarUsuario"
                        style="-fx-background-color: linear-gradient(to bottom right, #EF5350, #D32F2F);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 30;
                               -fx-effect: dropshadow(gaussian, rgba(211,47,47,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"/>

                <Button text="👤 Panel de Usuario" onAction="#handleVerUser"
                        style="-fx-background-color: linear-gradient(to bottom right, #AB47BC, #8E24AA);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 30;
                               -fx-cursor: hand;"/>

                <Button text="↩️ Volver" onAction="#handleVolver"
                        style="-fx-background-color: linear-gradient(to bottom right, #757575, #424242);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 30;
                               -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15;">
            <Label text="© 2025 Plataforma Logística | Gestión de Administradores"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.8;"/>
        </HBox>
    </bottom>
</BorderPane>
