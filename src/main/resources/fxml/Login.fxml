<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.LoginController"
            prefWidth="450" prefHeight="580"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <VBox alignment="CENTER" spacing="10" style="-fx-padding: 30 20 15 20;">
            <Label text="🚚 Sistema Logístico"
                   style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 2px;"/>
            <Label text="Plataforma de Envíos y Entregas"
                   style="-fx-font-size: 12px; -fx-text-fill: #80DEEA; -fx-opacity: 0.9;"/>
        </VBox>
    </top>

    <!-- FORMULARIO CENTRAL -->
    <center>
        <VBox alignment="CENTER" spacing="15" style="-fx-padding: 20;">
            <!-- Contenedor del formulario con estilo de tarjeta -->
            <VBox alignment="CENTER" spacing="15" style="-fx-background-color: rgba(255,255,255,0.95);
                                                           -fx-background-radius: 20;
                                                           -fx-padding: 30 40;
                                                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 15, 0, 0, 5);"
                  maxWidth="350">
                
                <Label text="Iniciar Sesión"
                       style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #01579B;"/>

                <!-- Campo Email -->
                <VBox spacing="8">
                    <Label text="📧 Correo Electrónico"
                           style="-fx-font-size: 13px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="emailField" promptText="ejemplo@correo.com"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 12; -fx-font-size: 14px;"
                               prefWidth="250"/>
                </VBox>

                <!-- Campo Contraseña -->
                <VBox spacing="8">
                    <Label text="🔒 Contraseña"
                           style="-fx-font-size: 13px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <HBox spacing="5" alignment="CENTER_LEFT">
                        <PasswordField fx:id="passwordField" promptText="••••••••"
                                       style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                              -fx-border-radius: 8; -fx-background-radius: 8;
                                              -fx-padding: 12; -fx-font-size: 14px;"
                                       HBox.hgrow="ALWAYS"/>
                        <TextField fx:id="passwordVisibleField" promptText="••••••••"
                                   style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                          -fx-border-radius: 8; -fx-background-radius: 8;
                                          -fx-padding: 12; -fx-font-size: 14px;"
                                   visible="false" managed="false"/>
                        <Button fx:id="togglePasswordButton" text="👁" 
                                onAction="#togglePasswordVisibility"
                                style="-fx-background-color: transparent; -fx-text-fill: #546E7A;
                                       -fx-font-size: 18px; -fx-padding: 8 12; -fx-cursor: hand;
                                       -fx-background-radius: 8;">
                            <tooltip>
                                <Tooltip text="Mostrar/Ocultar contraseña"/>
                            </tooltip>
                        </Button>
                    </HBox>
                </VBox>

                <!-- Botón Entrar -->
                <Button text="Entrar" onAction="#handleLogin"
                        style="-fx-background-color: linear-gradient(to right, #0277BD, #01579B);
                               -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 14 40;
                               -fx-effect: dropshadow(gaussian, rgba(1,87,155,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"
                        prefWidth="250"/>

                <!-- Mensaje de estado -->
                <Label fx:id="mensajeLabel" style="-fx-text-fill: #D32F2F; -fx-font-size: 12px; -fx-font-weight: bold;"/>

                <!-- Separador -->
                <Separator prefWidth="250" style="-fx-background-color: #CFD8DC;"/>

                <!-- Opciones secundarias -->
                <VBox spacing="12" alignment="CENTER">
                    <Button text="Registrarse" onAction="#handleAbrirRegistro"
                            style="-fx-background-color: transparent; -fx-text-fill: #0277BD;
                                   -fx-font-size: 14px; -fx-font-weight: bold; -fx-underline: true;
                                   -fx-cursor: hand;"/>
                    
                    <Button text="🔍 Rastrear Envío" onAction="#handleRastreo"
                            style="-fx-background-color: linear-gradient(to right, #757575, #616161);
                                   -fx-text-fill: white; -fx-font-size: 13px; -fx-font-weight: bold;
                                   -fx-background-radius: 8; -fx-padding: 10 30;
                                   -fx-cursor: hand;"
                            prefWidth="250"/>
                </VBox>
            </VBox>
        </VBox>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 20;">
            <Label text="© 2025 Plataforma Logística | Servicio de Entregas Rápidas"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.8;"/>
        </HBox>
    </bottom>
</BorderPane>
