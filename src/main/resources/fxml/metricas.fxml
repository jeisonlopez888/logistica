<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.chart.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.MetricasController"
            prefWidth="1450" prefHeight="900"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <VBox>
            <HBox alignment="CENTER_LEFT" spacing="20"
                  style="-fx-background-color: linear-gradient(to right, #FF7043, #E64A19);
                         -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);">
                <Label text="📊 Panel de Métricas Operativas"
                       style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 1px;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="🔄 Actualizar" onAction="#handleActualizar"
                        style="-fx-background-color: linear-gradient(to right, #66BB6A, #43A047);
                               -fx-text-fill: white; -fx-font-weight: bold;
                               -fx-background-radius: 8; -fx-padding: 10 20;
                               -fx-cursor: hand;"/>
                <Button text="↩️ Volver" onAction="#handleVolverAdmin"
                        style="-fx-background-color: linear-gradient(to right, #757575, #616161);
                               -fx-text-fill: white; -fx-font-weight: bold;
                               -fx-background-radius: 8; -fx-padding: 10 20;
                               -fx-cursor: hand;"/>
            </HBox>

            <!-- Filtros de fecha -->
            <HBox alignment="CENTER" spacing="20" style="-fx-padding: 15; -fx-background-color: rgba(255,255,255,0.1);">
                <Label text="📅 Fecha Inicio:" style="-fx-font-weight: bold; -fx-text-fill: white;"/>
                <DatePicker fx:id="fechaInicio" prefWidth="150"
                            style="-fx-background-color: white; -fx-border-radius: 5;"/>
                <Label text="📅 Fecha Fin:" style="-fx-font-weight: bold; -fx-text-fill: white;"/>
                <DatePicker fx:id="fechaFin" prefWidth="150"
                            style="-fx-background-color: white; -fx-border-radius: 5;"/>
            </HBox>
        </VBox>
    </top>

    <!-- CONTENIDO CENTRAL -->
    <center>
        <ScrollPane fitToWidth="true" fitToHeight="true" style="-fx-background: transparent; -fx-background-color: transparent;">
            <VBox spacing="20" style="-fx-padding: 20;">

                <!-- Métricas Generales -->
                <GridPane hgap="25" vgap="25" alignment="CENTER">
                    <columnConstraints>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                    </columnConstraints>

                    <!-- Tarjeta 1: Tiempo Promedio -->
                    <VBox alignment="CENTER" spacing="10" GridPane.columnIndex="0"
                          style="-fx-background-color: linear-gradient(to bottom, #42A5F5, #1E88E5);
                                 -fx-background-radius: 12; -fx-padding: 25;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);"
                          prefWidth="200">
                        <Label text="⏱️ Tiempo Promedio" style="-fx-font-size: 13px; -fx-text-fill: white; -fx-opacity: 0.9;"/>
                        <Label fx:id="tiempoPromedioLabel" text="0.0 días"
                               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
                    </VBox>

                    <!-- Tarjeta 2: Ingresos Totales -->
                    <VBox alignment="CENTER" spacing="10" GridPane.columnIndex="1"
                          style="-fx-background-color: linear-gradient(to bottom, #66BB6A, #43A047);
                                 -fx-background-radius: 12; -fx-padding: 25;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);"
                          prefWidth="200">
                        <Label text="💰 Ingresos Totales" style="-fx-font-size: 13px; -fx-text-fill: white; -fx-opacity: 0.9;"/>
                        <Label fx:id="ingresosTotalesLabel" text=" "
                               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
                    </VBox>

                    <!-- Tarjeta 3: Total Envíos -->
                    <VBox alignment="CENTER" spacing="10" GridPane.columnIndex="2"
                          style="-fx-background-color: linear-gradient(to bottom, #FFB300, #F57C00);
                                 -fx-background-radius: 12; -fx-padding: 25;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);"
                          prefWidth="200">
                        <Label text="📦 Total Envíos" style="-fx-font-size: 13px; -fx-text-fill: white; -fx-opacity: 0.9;"/>
                        <Label fx:id="totalEnviosLabel" text="0"
                               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
                    </VBox>

                    <!-- Tarjeta 4: Total Incidencias -->
                    <VBox alignment="CENTER" spacing="10" GridPane.columnIndex="3"
                          style="-fx-background-color: linear-gradient(to bottom, #EF5350, #D32F2F);
                                 -fx-background-radius: 12; -fx-padding: 25;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);"
                          prefWidth="200">
                        <Label text="⚠️ Incidencias" style="-fx-font-size: 13px; -fx-text-fill: white; -fx-opacity: 0.9;"/>
                        <Label fx:id="totalIncidenciasLabel" text="0"
                               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
                    </VBox>
                </GridPane>

                <!-- Gráficos -->
                <GridPane hgap="20" vgap="20">
                    <columnConstraints>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                        <ColumnConstraints halignment="CENTER" hgrow="ALWAYS"/>
                    </columnConstraints>
                    <rowConstraints>
                        <RowConstraints vgrow="ALWAYS"/>
                        <RowConstraints vgrow="ALWAYS"/>
                        <RowConstraints vgrow="ALWAYS"/>
                    </rowConstraints>

                    <!-- Gráfico 1: Tiempos de Entrega -->
                    <VBox GridPane.columnIndex="0" GridPane.rowIndex="0"
                          style="-fx-background-color: white; -fx-background-radius: 12; -fx-padding: 15;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);">
                        <LineChart fx:id="tiemposChart" title="Tiempos Promedio de Entrega por Zona" prefHeight="250" prefWidth="650">
                            <xAxis><CategoryAxis label="Zona"/></xAxis>
                            <yAxis><NumberAxis label="Días"/></yAxis>
                        </LineChart>
                    </VBox>

                    <!-- Gráfico 2: Servicios más usados -->
                    <VBox GridPane.columnIndex="1" GridPane.rowIndex="0"
                          style="-fx-background-color: white; -fx-background-radius: 12; -fx-padding: 15;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);">
                        <BarChart fx:id="serviciosChart" title="Servicios Adicionales" prefHeight="250" prefWidth="650">
                            <xAxis><CategoryAxis label="Servicio"/></xAxis>
                            <yAxis><NumberAxis label="Cantidad"/></yAxis>
                        </BarChart>
                    </VBox>

                    <!-- Gráfico 3: Distribución de Servicios (Pie) -->
                    <VBox GridPane.columnIndex="0" GridPane.rowIndex="1"
                          style="-fx-background-color: white; -fx-background-radius: 12; -fx-padding: 15;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);">
                        <PieChart fx:id="serviciosPieChart" title="Distribución de Servicios" prefHeight="250"/>
                    </VBox>

                    <!-- Gráfico 4: Ingresos -->
                    <VBox GridPane.columnIndex="1" GridPane.rowIndex="1"
                          style="-fx-background-color: white; -fx-background-radius: 12; -fx-padding: 15;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);">
                        <BarChart fx:id="ingresosChart" title="Ingresos por Período" prefHeight="250" prefWidth="650">
                            <xAxis><CategoryAxis label="Período"/></xAxis>
                            <yAxis><NumberAxis label="Ingresos ($)"/></yAxis>
                        </BarChart>
                    </VBox>

                    <!-- Gráfico 5: Incidencias por Zona -->
                    <VBox GridPane.columnIndex="0" GridPane.rowIndex="2" GridPane.columnSpan="2"
                          style="-fx-background-color: white; -fx-background-radius: 12; -fx-padding: 15;
                                 -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);">
                        <BarChart fx:id="incidenciasChart" title="Incidencias por Zona" prefHeight="250">
                            <xAxis><CategoryAxis label="Zona"/></xAxis>
                            <yAxis><NumberAxis label="Número de Incidencias"/></yAxis>
                        </BarChart>
                    </VBox>
                </GridPane>
            </VBox>
        </ScrollPane>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15; -fx-background-color: rgba(0,0,0,0.2);">
            <Label text="© 2025 Plataforma Logística | Dashboard de Métricas Operativas"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.9;"/>
        </HBox>
    </bottom>
</BorderPane>

