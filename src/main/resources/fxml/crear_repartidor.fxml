<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.CrearRepartidorController"
            prefWidth="500" prefHeight="550"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <HBox alignment="CENTER" spacing="15"
              style="-fx-background-color: linear-gradient(to right, #EF5350, #D32F2F);
                     -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);">
            <Label text="🚚 Registrar Repartidor"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 1px;"/>
        </HBox>
    </top>

    <!-- FORMULARIO CENTRAL -->
    <center>
        <VBox alignment="CENTER" spacing="20" style="-fx-padding: 30;">
            
            <!-- Tarjeta de formulario -->
            <VBox spacing="16" style="-fx-background-color: rgba(255,255,255,0.95);
                                      -fx-background-radius: 15;
                                      -fx-padding: 35;
                                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 15, 0, 0, 5);"
                  maxWidth="400">
                
                <Label text="Datos del Repartidor"
                       style="-fx-font-size: 18px; -fx-text-fill: #D32F2F; -fx-font-weight: bold;"/>

                <!-- Cédula/ID -->
                <VBox spacing="6">
                    <Label text="ID / Cédula *" style="-fx-font-size: 12px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="idField" promptText="Número de identificación"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 10; -fx-font-size: 13px;"/>
                </VBox>

                <!-- Documento -->
                <VBox spacing="6">
                    <Label text="Documento *" style="-fx-font-size: 12px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="documentoField" promptText="Número de documento"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 10; -fx-font-size: 13px;"/>
                </VBox>

                <!-- Nombre -->
                <VBox spacing="6">
                    <Label text="Nombre Completo *" style="-fx-font-size: 12px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="nombreField" promptText="Ej: Juan Pérez"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 10; -fx-font-size: 13px;"/>
                </VBox>

                <!-- Teléfono -->
                <VBox spacing="6">
                    <Label text="Teléfono *" style="-fx-font-size: 12px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="telefonoField" promptText="3001234567"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 10; -fx-font-size: 13px;"/>
                </VBox>

                <!-- Zona -->
                <VBox spacing="6">
                    <Label text="Zona de Trabajo *" style="-fx-font-size: 12px; -fx-text-fill: #546E7A; -fx-font-weight: bold;"/>
                    <TextField fx:id="zonaField" promptText="Norte, Centro, Sur"
                               style="-fx-background-color: white; -fx-border-color: #B0BEC5;
                                      -fx-border-radius: 8; -fx-background-radius: 8;
                                      -fx-padding: 10; -fx-font-size: 13px;"/>
                </VBox>

                <!-- Disponibilidad -->
                <CheckBox fx:id="disponibleCheck" text="Disponible para entregas"
                          style="-fx-font-size: 13px; -fx-text-fill: #37474F; -fx-font-weight: bold;"/>

                <Separator style="-fx-background-color: #CFD8DC;"/>

                <!-- Botón guardar -->
                <Button text="💾 Guardar" onAction="#handleGuardar"
                        style="-fx-background-color: linear-gradient(to right, #EF5350, #D32F2F);
                               -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 40;
                               -fx-effect: dropshadow(gaussian, rgba(211,47,47,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"
                        prefWidth="320"/>

                <!-- Mensaje -->
                <Label fx:id="mensajeLabel" wrapText="true" alignment="CENTER"
                       style="-fx-font-size: 13px; -fx-font-weight: bold;"/>
            </VBox>
        </VBox>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15;">
            <Label text="© 2025 Plataforma Logística | Gestión de Repartidores"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.8;"/>
        </HBox>
    </bottom>
</BorderPane>
