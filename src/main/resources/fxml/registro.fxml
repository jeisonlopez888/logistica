<?import javafx.scene.control.*?>
<?import javafx.scene.layout.VBox?>


<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="co.edu.uniquindio.logistica.ui.CrearUsuarioController"
      alignment="CENTER" spacing="10" prefWidth="400" prefHeight="600"
      style="-fx-background-color: #f9f9f9; -fx-padding: 20;">

    <Label text="Registro de Usuario" style="-fx-font-size: 20px; -fx-font-weight: bold;"/>

    <TextField fx:id="idField" promptText="Cédula"/>
    <TextField fx:id="nombreField" promptText="Nombre completo"/>
    <TextField fx:id="emailField" promptText="Correo electrónico"/>
    <TextField fx:id="telefonoField" promptText="Teléfono"/>
    <PasswordField fx:id="passwordField" promptText="Contraseña"/>

    <Separator/>
    <Label text="Dirección de Origen" style="-fx-font-weight: bold;"/>
    <TextField fx:id="alias1Field" promptText="Alias (ej: Casa)"/>
    <TextField fx:id="detalle1Field" promptText="Dirección completa"/>
    <!-- 🔹 Nueva lista desplegable para zona de origen -->
    <ComboBox fx:id="zonaOrigenCombo" promptText="Seleccione zona de origen (Sur, Centro, Norte)"/>

    <Separator/>
    <Label text="Dirección de Destino (opcional)" style="-fx-font-weight: bold;"/>
    <TextField fx:id="alias2Field" promptText="Alias (ej: Trabajo)"/>
    <TextField fx:id="detalle2Field" promptText="Dirección completa"/>
    <!-- 🔹 Nueva lista desplegable para zona de destino -->
    <ComboBox fx:id="zonaDestinoCombo" promptText="Seleccione zona de destino (Sur, Centro, Norte)"/>

    <Button text="Registrarse" onAction="#handleRegistrar"
            style="-fx-background-color: #1976D2; -fx-text-fill: white; -fx-background-radius: 8; -fx-padding: 6 12;"/>
    <Button text="↩️ Volver al Login" onAction="#handleVolverLogin"
            style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 8;"/>

    <Label fx:id="mensajeLabel"/>
</VBox>




