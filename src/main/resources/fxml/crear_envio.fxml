<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox alignment="CENTER" prefHeight="520" prefWidth="620" spacing="12" style="-fx-background-color: #f9f9f9; -fx-padding: 20;" xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/17.0.12" fx:controller="co.edu.uniquindio.logistica.ui.CrearEnvioController">

    <padding>
        <Insets bottom="20" left="20" right="20" top="20" />
    </padding>

    <Label style="-fx-font-size: 18px; -fx-font-weight: bold;" text="📦 Crear Envío" />

    <!-- 🔹 ORIGEN -->
    <Label text="Dirección de Origen:" />
    <TextField fx:id="origenDireccionField" promptText="Ej: Calle 10 # 5-20" />

    <Label text="Zona de Origen:" />
    <ComboBox fx:id="zonaOrigenCombo" promptText="Seleccione zona (Sur, Centro, Norte)" />

    <!-- 🔹 DESTINO -->
    <Label text="Dirección de Destino:" />
    <TextField fx:id="destinoDireccionField" promptText="Ej: Cra 12 # 4-15" />

    <Label text="Zona de Destino:" />
    <ComboBox fx:id="zonaDestinoCombo" promptText="Seleccione zona (Sur, Centro, Norte)" />

    <!-- 🔹 PESO Y OPCIONES -->
    <Label text="Peso del paquete (kg):" />
    <TextField fx:id="pesoField" promptText="Ej: 2.5" />

    <HBox spacing="20">
        <CheckBox fx:id="prioridadCheck" text="Envío Prioritario" />
        <CheckBox fx:id="seguroCheck" text="Agregar Seguro" />
    </HBox>

    <Separator />

    <!-- 🔹 Costo estimado -->
    <Label style="-fx-font-weight: bold;" text="Costo Estimado:" />
     <Button fx:id="crearBtn" disable="true" onAction="#handleCrear" style="-fx-background-color: #388E3C; -fx-text-fill: white; -fx-background-radius: 8; -fx-padding: 6 12;" text="Confirmar y Pagar" />
     <Button fx:id="cotizarBtn" onAction="#handleCotizar" style="-fx-background-color: #0288D1; -fx-text-fill: white; -fx-background-radius: 8; -fx-padding: 6 12;" text="Calcular Costo" />
    <Label fx:id="costoLabel" style="-fx-font-size: 16px; -fx-text-fill: #1976D2;" text="—" />

    <!-- 🔹 BOTONES -->
    <HBox alignment="CENTER" spacing="10">
        <Button fx:id="cancelarBtn" onAction="#handleCancelar" style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-background-radius: 8; -fx-padding: 6 12;" text="Cancelar" />
        <Button onAction="#handleVerDesglose" text="Ver detalle de tarifa" />
        <Button onAction="#handleGuardarEnvio" text="Guardar Envío" />
        <Button onAction="#handleVolverMenu" text="volver" />




    </HBox>

    <Label fx:id="mensajeLabel" style="-fx-font-size: 14px; -fx-text-fill: #1976D2;" />

</VBox>
