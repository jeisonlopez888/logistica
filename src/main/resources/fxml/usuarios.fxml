<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.effect.DropShadow?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.logistica.ui.UsuariosController"
            prefWidth="1000" prefHeight="650"
            style="-fx-background-color: linear-gradient(to bottom right, #f5f7fa, #e3f2fd);">
    <!-- 🔹 ENCABEZADO -->
    <top>
        <HBox alignment="CENTER_LEFT"
              spacing="20"
              style="-fx-background-color: #1976D2; -fx-padding: 25;">
            <Label text="📋 Gestión de Usuarios"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: white;"/>
        </HBox>
    </top>

    <!-- 🔹 TABLA CENTRAL -->
    <center>
        <VBox spacing="25" alignment="CENTER" style="-fx-padding: 30;">
            <Label text="Listado de Usuarios Registrados"
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #0D47A1;"/>

            <StackPane style="-fx-background-color: white; -fx-padding: 20; -fx-background-radius: 12;">
                <effect>
                    <DropShadow radius="10" color="#B0BEC5"/>
                </effect>
                <!-- 🔸 CAMBIO AQUÍ: fx:id="tablaUsuarios" para coincidir con el controlador -->
                <TableView fx:id="tablaUsuarios"
                           prefHeight="480" prefWidth="1000"
                           style="-fx-background-color: white; -fx-border-color: #E0E0E0; -fx-border-radius: 10; -fx-background-radius: 10;">
                    <columns>
                        <TableColumn fx:id="idCol" text="Cédula" prefWidth="100"/>
                        <TableColumn fx:id="nombreCol" text="Nombre" prefWidth="180"/>
                        <TableColumn fx:id="emailCol" text="Correo Electrónico" prefWidth="220"/>
                        <TableColumn fx:id="telefonoCol" text="Teléfono" prefWidth="150"/>
                        <TableColumn fx:id="passwordCol" text="Contraseña" prefWidth="150"/>
                        <!-- 🔸 CAMBIO AQUÍ: fx:id="rolCol" en lugar de adminCol -->
                        <TableColumn fx:id="rolCol" text="Administrador" prefWidth="120"/>
                        <TableColumn fx:id="direccionesCol" text="Direcciones" prefWidth="250"/>
                    </columns>
                </TableView>
            </StackPane>

            <Label fx:id="mensajeLabel" text=""
                   style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #1976D2;"/>
        </VBox>
    </center>

    <!-- 🔹 BOTONES ABAJO -->
    <bottom>
        <HBox alignment="CENTER" spacing="15" style="-fx-padding: 20;">
            <Button text="➕ Crear Usuario" onAction="#handleCrearUsuario"
                    style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-padding: 10 20;"/>
            <Button text="✏️ Editar Usuario" onAction="#handleEditarUsuario"
                    style="-fx-background-color: #FFC107; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-padding: 10 20;"/>
            <Button text="🗑️ Eliminar Usuario" onAction="#handleEliminarUsuario"
                    style="-fx-background-color: #F44336; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-padding: 10 20;"/>
            <Button text="↩️ Volver" onAction="#handleVolver"
                    style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-padding: 10 20;"/>
        </HBox>
    </bottom>

</BorderPane>

