<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1"
            xmlns="http://javafx.com/javafx/17.0.12"
            fx:controller="co.edu.uniquindio.logistica.ui.HistorialEnviosAdminController"
            prefHeight="750" prefWidth="1650"
            style="-fx-background-color: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);">

    <!-- ENCABEZADO -->
    <top>
        <VBox alignment="CENTER" spacing="8"
              style="-fx-background-color: linear-gradient(to right, #FF7043, #E64A19);
                     -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 4);">
            <Label text="📊 Todos los Envíos del Sistema - Administrador"
                   style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: white; -fx-letter-spacing: 1px;"/>
            <Label text="Gestión completa de todos los envíos - Edite estados y repartidores directamente en la tabla"
                   style="-fx-text-fill: #FFCCBC; -fx-font-size: 13px; -fx-opacity: 0.9;"/>
        </VBox>
    </top>

    <!-- CONTENIDO CENTRAL -->
    <center>
        <VBox spacing="15" style="-fx-padding: 20;">
            
            <!-- Tarjeta de tabla -->
            <VBox spacing="10" style="-fx-background-color: rgba(255,255,255,0.95);
                                      -fx-background-radius: 15;
                                      -fx-padding: 20;
                                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 12, 0, 0, 3);">
                
                <Label text="Todos los Envíos (tabla editable - doble clic para editar Estado o Repartidor)"
                       style="-fx-font-size: 16px; -fx-text-fill: #37474F; -fx-font-weight: bold;"/>

                <TableView fx:id="tablaEnvios" editable="true" prefHeight="550" style="-fx-border-radius: 10; -fx-background-radius: 10;">
                    <columns>
                        <TableColumn fx:id="colId" prefWidth="50" text="ID"/>
                        <TableColumn fx:id="colUsuario" prefWidth="120" text="Usuario"/>
                        
                        <!-- Origen -->
                        <TableColumn fx:id="colOrigenDireccion" prefWidth="140" text="Origen - Dir."/>
                        <TableColumn fx:id="colOrigenCiudad" prefWidth="85" text="Orig. Zona"/>
                        
                        <!-- Destino -->
                        <TableColumn fx:id="colDestinoDireccion" prefWidth="140" text="Destino - Dir."/>
                        <TableColumn fx:id="colDestinoCiudad" prefWidth="85" text="Dest. Zona"/>
                        
                        <!-- Detalles -->
                        <TableColumn fx:id="colPeso" prefWidth="70" text="Peso"/>
                        <TableColumn fx:id="colVolumen" prefWidth="70" text="Vol."/>
                        
                        <!-- Opciones adicionales -->
                        <TableColumn fx:id="colPrioridad" prefWidth="65" text="⚡ Prior."/>
                        <TableColumn fx:id="colSeguro" prefWidth="65" text="🛡️ Seg."/>
                        <TableColumn fx:id="colFragil" prefWidth="65" text="⚠️ Frágil"/>
                        <TableColumn fx:id="colFirmaRequerida" prefWidth="65" text="✍️ Firma"/>
                        
                        <!-- Estado y costo -->
                        <TableColumn fx:id="colEstado" prefWidth="110" text="Estado ✏️"/>
                        <TableColumn fx:id="colCostoEstimado" prefWidth="85" text="Costo"/>
                        
                        <!-- Fechas -->
                        <TableColumn fx:id="colFechaCreacion" prefWidth="110" text="Creación"/>
                        <TableColumn fx:id="colFechaConfirmacion" prefWidth="115" text="Confirm."/>
                        <TableColumn fx:id="colFechaEntregaEstimada" prefWidth="115" text="Est. Entr."/>
                        <TableColumn fx:id="colFechaEntrega" prefWidth="110" text="Entrega"/>
                        
                        <!-- Repartidor -->
                        <TableColumn fx:id="colRepartidor" prefWidth="150" text="Repartidor ✏️" editable="true"/>
                    </columns>
                </TableView>
            </VBox>

            <!-- Botones de acción -->
            <HBox alignment="CENTER" spacing="18" style="-fx-padding: 10 0 0 0;">
                <Button text="✏️ Editar Envío" onAction="#editarEnvio"
                        style="-fx-background-color: linear-gradient(to bottom right, #42A5F5, #1E88E5);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 25;
                               -fx-effect: dropshadow(gaussian, rgba(30,136,229,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"/>

                <Button text="🗑️ Eliminar Envío" onAction="#eliminarEnvio"
                        style="-fx-background-color: linear-gradient(to bottom right, #EF5350, #D32F2F);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 25;
                               -fx-effect: dropshadow(gaussian, rgba(211,47,47,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"/>

                <Button text="⚠️ Ver/Registrar Incidencia" onAction="#verIncidencia"
                        style="-fx-background-color: linear-gradient(to bottom right, #FFB300, #F57C00);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 25;
                               -fx-effect: dropshadow(gaussian, rgba(245,124,0,0.5), 8, 0, 0, 3);
                               -fx-cursor: hand;"/>

                <Button text="↩️ Volver" onAction="#handleVolver"
                        style="-fx-background-color: linear-gradient(to bottom right, #757575, #424242);
                               -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;
                               -fx-background-radius: 10; -fx-padding: 12 25;
                               -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </center>

    <!-- PIE DE PÁGINA -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15;">
            <Label text="© 2025 Plataforma Logística | Panel de Administración - Gestión de Todos los Envíos"
                   style="-fx-text-fill: #B3E5FC; -fx-font-size: 11px; -fx-opacity: 0.8;"/>
        </HBox>
    </bottom>
</BorderPane>
